#include <unistd.h>
#include <stdlib.h>
// #include <stdio.h>

int main() {
    //READ
    // char buff[128];
    // int nread;
    // nread = read(0, buff, 128);
    // if(nread == -1) {
    //     write(2, "Error\n", 6);
    //     exit(0);
    // }
    // if(write(1, buff, nread) != nread) {
    //     write(2, "Error\n", 6);
    //     exit(0);
    // }
    return 0;
}